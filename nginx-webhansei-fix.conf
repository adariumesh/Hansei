server {
    listen 80;
    listen [::]:80;
    server_name webhansei.us www.webhansei.us;

    root /var/www/hansei;
    index index.html index-3d.html;

    # Enable gzip compression
    gzip on;
    gzip_vary on;
    gzip_min_length 1024;
    gzip_types text/plain text/css text/xml text/javascript application/javascript application/json;

    # 3D visualization route - THE CRITICAL FIX
    location = /3d {
        try_files /index-3d.html =404;
        add_header Cache-Control "public, max-age=3600";
    }

    # Main route
    location / {
        try_files $uri $uri/ =404;
        add_header Cache-Control "public, max-age=3600";
    }

    # Security headers
    add_header X-Frame-Options "SAMEORIGIN" always;
    add_header X-Content-Type-Options "nosniff" always;
    add_header X-XSS-Protection "1; mode=block" always;

    # Logs
    access_log /var/log/nginx/webhansei-access.log;
    error_log /var/log/nginx/webhansei-error.log;
}

# HTTPS redirect server block (for future SSL setup)
server {
    listen 443 ssl http2;
    listen [::]:443 ssl http2;
    server_name webhansei.us www.webhansei.us;

    # SSL certificate paths (update these when SSL is configured)
    # ssl_certificate /path/to/certificate.pem;
    # ssl_certificate_key /path/to/private.key;

    root /var/www/hansei;
    index index.html index-3d.html;

    # Enable gzip compression
    gzip on;
    gzip_vary on;
    gzip_min_length 1024;
    gzip_types text/plain text/css text/xml text/javascript application/javascript application/json;

    # 3D visualization route - THE CRITICAL FIX
    location = /3d {
        try_files /index-3d.html =404;
        add_header Cache-Control "public, max-age=3600";
    }

    # Main route
    location / {
        try_files $uri $uri/ =404;
        add_header Cache-Control "public, max-age=3600";
    }

    # Security headers
    add_header X-Frame-Options "SAMEORIGIN" always;
    add_header X-Content-Type-Options "nosniff" always;
    add_header X-XSS-Protection "1; mode=block" always;

    # Logs
    access_log /var/log/nginx/webhansei-access.log;
    error_log /var/log/nginx/webhansei-error.log;
}